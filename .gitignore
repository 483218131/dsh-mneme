# dependencies
node_modules/

# build / logs
*.log
.DS_Store
dist/
build/
coverage/

# secrets & credentials — NEVER commit these
.env
.env.*
!.env.example
*.secret
*credential*
*credentials.json
id_rsa*
*.pem
*.key
*.p12
*.pfx

# runtime/local data (memory.db lives in ~/.dsh, but guard anyway)
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
memory.db

# editor / OS
.vscode/
.idea/
*.swp

# 并行工作区车道（git worktree add .worktrees/<lane>）：见 AGENTS.md
.worktrees/

# 本机个人工具（dsh 目录镜像滞后监控等，非包内容，不进版本库）
tools/
